<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leaves Taken Report</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.4;
            color: #333;
            background-color: #fff;
            padding: 15px;
        }
        
        .container {
            max-width: 1000px;
            margin: 0 auto;
            background-color: white;
            padding: 25px;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 3px solid #2c3e50;
            padding-bottom: 15px;
        }
        
        .header h1 {
            color: #2c3e50;
            font-size: 24px;
            margin-bottom: 8px;
            font-weight: 600;
        }
        
        .header .subtitle {
            color: #7f8c8d;
            font-size: 12px;
            font-style: italic;
        }
        
        .company-info {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 6px;
            margin-bottom: 25px;
            border-left: 4px solid #3498db;
        }
        
        .company-info .detail {
            margin-bottom: 6px;
            font-size: 13px;
        }
        
        .company-info .label {
            font-weight: 600;
            color: #555;
            display: inline-block;
            width: 100px;
        }
        
        .company-info .value {
            color: #333;
        }
        
        .summary-stats {
            display: flex;
            justify-content: space-around;
            margin-bottom: 25px;
            padding: 15px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 6px;
            color: white;
        }
        
        .stat-item {
            text-align: center;
        }
        
        .stat-value {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 4px;
        }
        
        .stat-label {
            font-size: 10px;
            text-transform: uppercase;
            opacity: 0.9;
        }
        
        .table-container {
            margin-top: 20px;
            border: 1px solid #e0e0e0;
            border-radius: 6px;
            overflow: hidden;
        }
        
        .table-header {
            background-color: #34495e;
            color: white;
            padding: 12px;
        }
        
        .table-header h3 {
            font-size: 16px;
            font-weight: 500;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            background-color: white;
            font-size: 11px;
        }
        
        thead {
            background-color: #ecf0f1;
        }
        
        th {
            padding: 10px 8px;
            text-align: left;
            font-weight: 600;
            color: #2c3e50;
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.3px;
            border-bottom: 2px solid #bdc3c7;
            border-right: 1px solid #ddd;
        }
        
        th:last-child {
            border-right: none;
        }
        
        tbody tr {
            border-bottom: 1px solid #f0f0f0;
        }
        
        tbody tr:hover {
            background-color: #f8f9fa;
        }
        
        td {
            padding: 8px;
            font-size: 11px;
            color: #555;
            border-right: 1px solid #f0f0f0;
            vertical-align: top;
        }
        
        td:last-child {
            border-right: none;
        }
        
        .leave-type-cell {
            font-weight: 600;
            color: #2c3e50;
            background-color: #f8f9fa;
            border-left: 3px solid #3498db;
        }
        
        .department-cell {
            font-weight: 500;
            color: #555;
            background-color: #fdfdfd;
            padding-left: 12px;
        }
        
        .employee-entry {
            margin-bottom: 2px;
            font-size: 10px;
        }
        
        .employee-name {
            font-weight: 500;
            color: #2c3e50;
        }
        
        .employee-days {
            color: #7f8c8d;
            font-weight: 400;
        }
        
        .department-total {
            font-weight: 600;
            color: #e74c3c;
            font-size: 12px;
            text-align: center;
        }
        
        .empty-cell {
            background-color: #fafafa;
        }
        
        .no-data {
            text-align: center;
            padding: 30px;
            color: #95a5a6;
            font-style: italic;
        }
        
        .footer {
            margin-top: 30px;
            padding-top: 15px;
            border-top: 1px solid #ecf0f1;
            text-align: center;
            color: #95a5a6;
            font-size: 10px;
        }
        
        .footer .timestamp {
            margin-bottom: 4px;
        }
        
        .footer .confidential {
            color: #e74c3c;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-top: 8px;
        }
        
        @media print {
            body {
                padding: 0;
            }
            
            .container {
                padding: 15px;
                max-width: 100%;
            }
            
            table {
                page-break-inside: auto;
            }
            
            tr {
                page-break-inside: avoid;
                page-break-after: auto;
            }
            
            thead {
                display: table-header-group;
            }
            
            .leave-type-row {
                page-break-before: auto;
            }
        }
        
        /* Specific styling for the nested table structure */
        .leave-type-row {
            border-top: 2px solid #3498db;
        }
        
        .leave-type-row td {
            padding: 10px 8px;
        }
        
        .department-row td {
            padding: 6px 8px;
        }
        
        .total-summary {
            background-color: #2c3e50;
            color: white;
            font-weight: 600;
        }
        
        .total-summary td {
            padding: 10px 8px;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>Leaves Taken Report</h1>
            <div class="subtitle">Employee Leave Analysis by Department and Leave Type</div>
        </div>
        
        <!-- Company Information -->
        <div class="company-info">
            <div class="detail">
                <span class="label">Company:</span>
                <span class="value">{{companyName}}</span>
            </div>
            <div class="detail">
                <span class="label">Report Period:</span>
                <span class="value">{{reportPeriod}}</span>
            </div>
            <div class="detail">
                <span class="label">Generated:</span>
                <span class="value">{{currentDate}}</span>
            </div>
        </div>
        
        <!-- Summary Statistics -->
        <div class="summary-stats">
            <div class="stat-item">
                <div class="stat-value">{{totalLeaveTypes}}</div>
                <div class="stat-label">Leave Types</div>
            </div>
            <div class="stat-item">
                <div class="stat-value">{{totalDepartments}}</div>
                <div class="stat-label">Departments</div>
            </div>
            <div class="stat-item">
                <div class="stat-value">{{totalEmployees}}</div>
                <div class="stat-label">Employees</div>
            </div>
            <div class="stat-item">
                <div class="stat-value">{{totalDays}}</div>
                <div class="stat-label">Total Days</div>
            </div>
        </div>
        
        <!-- Main Report Table -->
        <div class="table-container">
            <div class="table-header">
                <h3>Leave Days by Type and Department</h3>
            </div>
            
            {{#if reportData.length}}
            <table>
                <thead>
                    <tr>
                        <th style="width: 20%;">Leave Type</th>
                        <th style="width: 18%;">Department</th>
                        <th style="width: 45%;">Employees</th>
                        <th style="width: 17%;">Department Total</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each reportData}}
                    {{#each this.department}}
                    {{#if @first}}
                    <!-- First department row for this leave type -->
                    <tr class="leave-type-row">
                        <td class="leave-type-cell" rowspan="{{../this.department.length}}">
                            <strong>{{../this.leaveType.name}}</strong>
                            {{#if ../this.leaveType.code}}
                            <br><small>({{../this.leaveType.code}})</small>
                            {{/if}}
                        </td>
                        <td class="department-cell">
                            <strong>{{#if this.name}}{{this.name}}{{else}}No Department{{/if}}</strong>
                            {{#if this.code}}
                            <br><small>({{this.code}})</small>
                            {{else}}
                            {{#unless this.name}}
                            <br><small>(NODEPARTMENT)</small>
                            {{/unless}}
                            {{/if}}
                        </td>
                        <td>
                            {{#each this.employees}}
                            <div class="employee-entry">
                                <span class="employee-name">{{this.name}}</span>
                                <span class="employee-days">({{this.numberOfDays}} days)</span>
                            </div>
                            {{/each}}
                        </td>
                        <td class="department-total">{{this.numberOfDaysPerDepartment}} days</td>
                    </tr>
                    {{else}}
                    <!-- Subsequent department rows for this leave type -->
                    <tr class="department-row">
                        <td class="department-cell">
                            <strong>{{#if this.name}}{{this.name}}{{else}}No Department{{/if}}</strong>
                            {{#if this.code}}
                            <br><small>({{this.code}})</small>
                            {{else}}
                            {{#unless this.name}}
                            <br><small>(NODEPARTMENT)</small>
                            {{/unless}}
                            {{/if}}
                        </td>
                        <td>
                            {{#each this.employees}}
                            <div class="employee-entry">
                                <span class="employee-name">{{this.name}}</span>
                                <span class="employee-days">({{this.numberOfDays}} days)</span>
                            </div>
                            {{/each}}
                        </td>
                        <td class="department-total">{{this.numberOfDaysPerDepartment}} days</td>
                    </tr>
                    {{/if}}
                    {{/each}}
                    
                    <!-- Leave Type Total Row -->
                    <tr class="total-summary">
                        <td colspan="3" style="text-align: right; padding-right: 20px;">
                            <strong>Total for {{this.leaveType.name}}:</strong>
                        </td>
                        <td style="text-align: center;">
                            <strong>{{this.numberOfDaysPerCompany}} days</strong>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            {{else}}
            <div class="no-data">
                No leave data available for the selected period.
            </div>
            {{/if}}
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <div class="timestamp">Report generated on {{currentDate}}</div>
            <div class="company">{{companyName}}</div>
            <div class="confidential">Confidential - Internal Use Only</div>
        </div>
    </div>
</body>
</html>